---
title: Download Lewdware
description: Download Lewdware, the config app, and the lw CLI for your platform.
---

import DownloadSection from '../../../components/DownloadSection.astro';
import latest from '../../../data/latest.json';

This installer includes:

- **lewdware** — the main app
- **lewdware-config** — the configuration GUI
- **lw** — the CLI for building and testing modes

:::note[Windows SmartScreen]
On Windows, the installer is not code-signed. When prompted by SmartScreen, click **More info** →
**Run anyway** to proceed.
:::

:::note[macOS Gatekeeper]
The installer is not notarized. macOS will block it on first open.

After the first blocked attempt, open **System Settings → Privacy & Security**,
scroll to the security section at the bottom, and click **Open Anyway** next to the blocked
installer entry.
:::

<DownloadSection
  windowsUrl={latest.assets['windows-x64']}
  windowsExt="exe"
  macosArm64Url={latest.assets['macos-arm64']}
  macosX64Url={latest.assets['macos-x64']}
  macosExt="pkg"
  linuxFormats={[
    {
      label: '.deb',
      desc: 'Debian, Ubuntu, and derivatives',
      x64: latest.assets['linux-x64-deb'],
      arm64: latest.assets['linux-arm64-deb'],
    },
    {
      label: '.rpm',
      desc: 'Fedora, RHEL, openSUSE, and derivatives',
      x64: latest.assets['linux-x64-rpm'],
      arm64: latest.assets['linux-arm64-rpm'],
    },
    {
      label: '.tar.gz',
      desc: 'Portable, any distribution',
      x64: latest.assets['linux-x64-tar'],
      arm64: latest.assets['linux-arm64-tar'],
    },
  ]}
/>
